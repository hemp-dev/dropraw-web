# Metadata Privacy

Default metadata mode: `strip`. Он удаляет GPS/private camera data, где это поддерживается encoder.

Никогда не публикуйте Dropbox tokens, Google credentials, AWS secrets, OAuth refresh tokens, Git hosting tokens или CI secrets.
