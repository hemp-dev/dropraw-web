# Quick Start

```bash
rawbridge --version
rawbridge doctor
rawbridge scan ./RAW
rawbridge convert ./RAW --out ./web_export --preset web
rawbridge ui
```

Resume state хранится в `.rawbridge_manifest.sqlite`.
