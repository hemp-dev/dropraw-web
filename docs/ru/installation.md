# Установка

```bash
pip install rawbridge
```

Dev install:

```bash
git clone https://github.com/hemp-dev/rawbridge.git
cd rawbridge
python3.12 -m venv .venv
source .venv/bin/activate
pip install -e ".[dev]"
```

Python 3.14 не recommended для этого релиза.
