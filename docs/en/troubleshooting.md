# Troubleshooting

- Run `rawbridge doctor`.
- Use Python 3.11 or 3.12.
- Increase `--list-retries`, `--download-retries`, and `--retry-delay`.
- Use `--only-failed ./web_export/rawbridge_failed.tsv`.
- Check AVIF support in `rawbridge doctor`.
- Never paste credentials into public issues.
