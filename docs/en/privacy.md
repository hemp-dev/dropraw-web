# Metadata Privacy

Default metadata mode is `strip`. It removes GPS and private camera data where supported by encoders.

Never publish Dropbox tokens, Google credentials, AWS secrets, OAuth refresh tokens, Git hosting tokens, or CI secrets.
